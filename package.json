{
  "name": "hermes-agent-notes",
  "version": "0.1.36",
  "description": "Obsidian plugin: write Markdown notes with a local or remote Hermes Agent instance.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node scripts/build-tests.mjs && node scripts/selftest.mjs && node scripts/mock-server-test.mjs && node scripts/plugin-test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.19.0",
    "js-yaml": "^4.3.2",
    "obsidian": "^1.4.0",
    "typescript": "^5.3.0"
  }
}
